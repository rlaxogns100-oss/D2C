# 🍔 Food Delivery App - D2C Builder

Figma 디자인을 HTML/CSS로 변환한 음식 주문 앱 프로젝트입니다.

## 📁 프로젝트 구조

```
D2C_builder/
├── loading.html        # 로딩/스플래시 화면
├── login.html          # 로그인 화면
├── signup.html         # 회원가입 화면
├── location.html       # 위치 접근 권한 화면
├── index.html          # Home 화면 (레스토랑 카드 목록)
├── menu.html           # Home_Menu 화면 (드롭다운 메뉴)
├── details.html        # Food_Details 화면 (음식 상세)
├── orders.html         # My_Order 화면 (주문 내역)
├── address.html        # Address 화면 (주소 목록)
├── add-address.html    # Add_New_Address 화면 (주소 추가)
├── cart.html           # My_Cart 화면 (장바구니)
├── style.css           # 전체 스타일 시스템
├── assets/
│   ├── icons/          # SVG 아이콘 파일들
│   │   ├── menu.svg
│   │   ├── back.svg
│   │   ├── heart.svg
│   │   ├── star.svg
│   │   ├── truck.svg
│   │   ├── clock.svg
│   │   ├── location.svg
│   │   ├── chevron-down.svg
│   │   ├── plus.svg
│   │   └── minus.svg
│   └── images/         # 이미지 파일 (나중에 추가)
└── README.md           # 이 파일
```

## 🚀 실행 방법

### 방법 1: 직접 파일 열기
1. `index.html` 파일을 더블클릭
2. 브라우저에서 자동으로 열립니다

### 방법 2: Live Server 사용 (추천)
1. VS Code 설치
2. "Live Server" 확장 프로그램 설치
3. `index.html` 우클릭 → "Open with Live Server"

### 방법 3: Python 서버
```bash
# Python 3
python3 -m http.server 8000

# 브라우저에서 http://localhost:8000 접속
```

## 📱 화면 구성 (총 11개)

### 인증 & 온보딩 (4개)
1. **Loading** (`loading.html`) - 스플래시 화면, 앱 로고
2. **Log_In** (`login.html`) - 이메일/비밀번호 로그인, 소셜 로그인
3. **Sign_Up** (`signup.html`) - 회원가입 폼
4. **Location_Access** (`location.html`) - 위치 권한 요청

### 메인 화면 (3개)
5. **Home** (`index.html`) - 레스토랑 목록, 카테고리 탭, 제품 그리드
6. **Home_Menu** (`menu.html`) - Home + 드롭다운 메뉴
7. **Food_Details** (`details.html`) - 음식 상세, 사이즈/수량 선택

### 주문 & 배송 (4개)
8. **My_Order** (`orders.html`) - 주문 내역, Ongoing/History 탭
9. **Address** (`address.html`) - 저장된 주소 목록
10. **Add_New_Address** (`add-address.html`) - 주소 추가, 지도
11. **My_Cart** (`cart.html`) - 장바구니, 주문하기

## 🎨 디자인 시스템

### 색상
- **Primary Orange**: `#FF8A3D` (버튼, 강조)
- **Background Gray**: `#F5F5F5` (배경)
- **Card Gradient**: `#9CADB7` → `#7A8F9D` (카드 배경)
- **Text Dark**: `#2D2D2D` (본문)
- **Text Gray**: `#9E9E9E` (보조 텍스트)

### 타이포그래피
- 시스템 폰트 스택 사용
- H1: 28px / H2: 24px / H3: 18px
- Body: 14px

### 간격 시스템
- XS: 8px / SM: 12px / MD: 16px / LG: 24px / XL: 32px

### Border Radius
- Small: 8px / Medium: 12px / Large: 16px / XL: 24px

## 📐 반응형 디자인

### 모바일 (기본)
- 최대 너비: 480px
- 전체 화면 사용

### 데스크톱
- 최대 480px로 제한
- 화면 중앙 정렬
- 양옆 여백 자동

## 🔧 커스터마이징

### 색상 변경
`style.css`의 `:root` 섹션에서 CSS 변수 수정:

```css
:root {
  --primary-orange: #FF8A3D;  /* 원하는 색상으로 변경 */
  --background-gray: #F5F5F5;
  /* ... */
}
```

### 이미지 추가
1. `assets/images/` 폴더에 이미지 업로드
2. HTML에서 `.image-card` 스타일 수정:

```css
.image-card {
  background-image: url('./assets/images/your-image.jpg');
  background-size: cover;
  background-position: center;
}
```

또는 HTML에서 직접:

```html
<div class="image-card" style="background-image: url('./assets/images/burger.jpg');">
  <!-- ... -->
</div>
```

### 아이콘 변경
- `assets/icons/` 폴더의 SVG 파일 수정
- 또는 HTML의 인라인 SVG 직접 수정

## 🎯 다음 단계

### 기능 추가 (JavaScript 필요)
- [ ] 드롭다운 메뉴 토글
- [ ] 수량 증감 버튼
- [ ] 사이즈 선택
- [ ] 즐겨찾기 토글
- [ ] 탭 전환
- [ ] 페이지네이션 슬라이더
- [ ] 장바구니 추가
- [ ] 검색 기능

### 최적화
- [ ] 이미지 압축
- [ ] CSS 미니파이
- [ ] 웹 접근성 개선 (ARIA 레이블)
- [ ] SEO 최적화

## 💡 팁

### 브라우저 호환성
- Chrome, Safari, Firefox, Edge 최신 버전 지원
- IE는 지원하지 않습니다

### 개발 도구
- Chrome DevTools로 모바일 뷰 테스트
- Responsive Design Mode 사용

### 디버깅
- 화면 왼쪽 상단에 현재 화면 이름 표시
- 개발 완료 후 `.screen-label` 제거

## 📞 문의

추가 화면이나 기능이 필요하시면 언제든지 알려주세요!

---

**제작**: Cursor AI Assistant  
**날짜**: 2025년 12월 14일

